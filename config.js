const config = {
    files: {
        players: "players.json",
        logs: "logs.txt",
    },

    player: {
        defaultMoney: 1000,
    },

    roles: {
        user: "user",
        admin: "admin",
    },
};

module.exports = config;